# 🔧 Настройка доступа к PostgreSQL на Beget

## Проблема
Подключение к БД не работает: `ECONNREFUSED` или `ETIMEDOUT`

## Решение

### Шаг 1: Проверьте настройки БД в панели Beget

1. Войдите в панель Beget: https://cp.beget.com
2. Перейдите в раздел **Cloud → Databases**
3. Найдите вашу БД `arteco_db`
4. Откройте настройки БД

### Шаг 2: Проверьте правильный хост для подключения

В панели Beget найдите:
- **Внутренний хост** (для подключения с сервера)
- **Внешний хост** (для подключения извне)
- **Порт** (может быть не 5432!)

**Важно:** На Beget PostgreSQL может быть доступен:
- Только через **localhost** (если БД на том же сервере)
- Через **специальный порт** (не стандартный 5432)
- Через **прокси** или **туннель**

### Шаг 3: Попробуйте разные варианты подключения

#### Вариант A: Localhost (если БД на том же сервере)

```bash
# На сервере проверьте, доступна ли БД локально
psql -h localhost -U arteco -d arteco_db
```

Если работает, используйте в `.env`:
```env
DATABASE_URL=postgresql://arteco:8926416Salavat1996s%40@localhost:5432/arteco_db
DATABASE_SSL=false
```

#### Вариант B: Через Unix socket (если доступен)

```bash
# Проверьте, есть ли socket файл
ls -la /var/run/postgresql/
# или
ls -la /tmp/.s.PGSQL.5432
```

Если есть, используйте:
```env
DATABASE_URL=postgresql://arteco:8926416Salavat1996s%40@/arteco_db?host=/var/run/postgresql
DATABASE_SSL=false
```

#### Вариант C: Через панель Beget (SQL редактор)

1. В панели Beget найдите вашу БД
2. Откройте **SQL редактор** или **phpPgAdmin**
3. Скопируйте содержимое `database/schema.sql`
4. Выполните SQL через панель

### Шаг 4: Проверьте настройки firewall

На Beget может быть ограничен доступ к порту 5432. Проверьте:

```bash
# На сервере проверьте доступность порта
telnet localhost 5432
# или
nc -zv localhost 5432
```

### Шаг 5: Альтернативное решение - использовать файловую систему

Если подключение к БД не работает, приложение автоматически переключится на файловую систему (JSON файлы).

Это временное решение, пока не настроите БД правильно.

## Что нужно сделать:

1. **Откройте панель Beget** и проверьте точные данные подключения
2. **Попробуйте подключиться через psql** с разных хостов:
   ```bash
   # Локально
   psql -h localhost -U arteco -d arteco_db
   
   # Через приватный IP
   psql -h 10.19.0.1 -U arteco -d arteco_db
   
   # Через публичный хост
   psql -h jogelfilulmir.beget.app -p 5432 -U arteco -d arteco_db
   ```

3. **Какой вариант работает?** Используйте его в `.env`

4. **Если ни один не работает:**
   - Проверьте настройки БД в панели Beget
   - Возможно, нужно открыть порт в firewall
   - Или использовать SQL редактор в панели Beget

## Временное решение

Пока настраиваете БД, приложение будет работать с файловой системой (JSON файлы в `data/`). Это нормально для начала.
