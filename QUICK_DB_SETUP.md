# 🚀 Быстрая настройка базы данных

## Рекомендация: PostgreSQL 16.4

Выберите **PostgreSQL 16.4** в Beget Cloud Databases, потому что:
- ✅ Совместим с Supabase (который уже в проекте)
- ✅ Современная версия с лучшей производительностью
- ✅ Поддержка расширений (pgvector и др.)

## Шаги настройки:

### 1. Создайте базу данных в Beget

1. Перейдите: https://cp.beget.com/cloud/databases/create
2. Выберите **PostgreSQL 16.4**
3. Регион: **Россия, Москва**
4. Конфигурация: минимум **2 ГБ RAM** (рекомендуется)
5. Создайте БД

### 2. Получите данные подключения

После создания вы получите:
- Host (хост)
- Port (порт, обычно 5432)
- Database (имя БД)
- Username (пользователь)
- Password (пароль)

### 3. Выполните SQL миграцию

Подключитесь к БД через панель Beget или psql и выполните:

```bash
# Скопируйте содержимое файла database/schema.sql
# И выполните в SQL редакторе Beget или через psql:
psql -h YOUR_HOST -U YOUR_USERNAME -d YOUR_DATABASE -f database/schema.sql
```

### 4. Настройте Supabase (рекомендуется)

**Вариант A: Использовать Supabase Cloud (бесплатно до 500MB)**

1. Создайте аккаунт: https://supabase.com
2. Создайте проект
3. В настройках проекта найдите:
   - Project URL → `NEXT_PUBLIC_SUPABASE_URL`
   - anon public key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - service_role key → `SUPABASE_SERVICE_ROLE_KEY`

**Вариант B: Использовать БД Beget напрямую**

Если хотите использовать БД Beget напрямую, нужно настроить Supabase для работы с внешней БД или использовать библиотеку `pg`.

### 5. Настройте переменные окружения

**На локальной машине:**
```bash
cp env.template .env.local
# Отредактируйте .env.local и добавьте ваши ключи
```

**На сервере:**
```bash
ssh root@155.212.147.140
cd /var/www/arteconew
nano .env
# Добавьте переменные:
NEXT_PUBLIC_SUPABASE_URL=your-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### 6. Перезапустите приложение

```bash
pm2 restart arteconew
pm2 logs arteconew
```

## Хранение изображений

### Вариант 1: Supabase Storage (рекомендуется)

1. В Supabase создайте bucket `images`
2. Настройте политики: public read
3. Используйте Supabase Storage API для загрузки

### Вариант 2: Локальное хранилище

```bash
# На сервере
mkdir -p /var/www/arteconew/public/images
chmod 755 /var/www/arteconew/public/images
```

## Проверка работы

1. Откройте админ-панель: http://155.212.147.140:3000/admin
2. Попробуйте добавить товар
3. Проверьте, что данные сохраняются в БД

## Fallback на файловую систему

Если БД не настроена, приложение автоматически использует файловую систему (JSON файлы) как резервный вариант. Это позволяет работать без БД на этапе разработки.
