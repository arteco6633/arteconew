# 🚀 Настройка Supabase

## Шаг 1: Создание проекта Supabase

1. Перейдите на https://supabase.com
2. Войдите или зарегистрируйтесь
3. Нажмите **"New Project"**
4. Заполните:
   - **Name**: `arteconew` (или любое другое имя)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
5. Нажмите **"Create new project"**

## Шаг 2: Получение ключей

После создания проекта:

1. Перейдите в **Settings** → **API**
2. Найдите:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** ключ → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** ключ → это `SUPABASE_SERVICE_ROLE_KEY` (⚠️ секретный!)

## Шаг 3: Создание таблиц

1. Перейдите в **SQL Editor** в панели Supabase
2. Скопируйте содержимое файла `database/schema.sql`
3. Вставьте в SQL Editor
4. Нажмите **"Run"**

Или выполните через терминал:

```bash
# Установите Supabase CLI (опционально)
npm install -g supabase

# Или просто скопируйте SQL из database/schema.sql
# и выполните в SQL Editor панели Supabase
```

## Шаг 4: Настройка переменных окружения

### На локальной машине:

```bash
cp env.template .env.local
nano .env.local
```

Добавьте:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### На сервере:

```bash
ssh root@155.212.147.140
cd /var/www/arteconew
nano .env
```

Добавьте те же переменные:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

## Шаг 5: Проверка

### На сервере:

```bash
cd /var/www/arteconew
pm2 restart arteconew --update-env
pm2 logs arteconew --lines 30
```

### Через браузер:

1. Откройте: http://155.212.147.140:3000/admin
2. Попробуйте добавить товар
3. Если сохранилось - всё работает! ✅

## Важно:

- ✅ `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` - публичные, можно использовать в клиентском коде
- ⚠️ `SUPABASE_SERVICE_ROLE_KEY` - секретный! Только для серверной части, НИКОГДА не используйте в клиентском коде!
- ✅ Не коммитьте `.env` файл в Git (он уже в .gitignore)

## Troubleshooting:

### Ошибка "relation does not exist":
Выполните SQL миграцию из `database/schema.sql` в SQL Editor Supabase

### Ошибка подключения:
Проверьте правильность ключей в `.env` файле

### Приложение не видит данные:
Убедитесь, что таблицы созданы и в них есть данные (можно проверить в Table Editor в панели Supabase)
