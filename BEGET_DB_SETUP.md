# 🎯 Настройка PostgreSQL БД на Beget (Рекомендуется!)

## Почему это лучше:

✅ **Данные на вашем сервере** - полный контроль  
✅ **Нет зависимости от внешних сервисов**  
✅ **Быстрее** - БД в той же сети/регионе  
✅ **Дешевле** - не нужно платить за Supabase Cloud  
✅ **Безопаснее** - данные не покидают ваш сервер  

## Шаг 1: Создание БД в Beget

1. Перейдите: https://cp.beget.com/cloud/databases/create
2. Выберите **PostgreSQL 16.4** ✅
3. Регион: **Россия, Москва**
4. Конфигурация: минимум **2 ГБ RAM** (рекомендуется)
5. Нажмите "Создать"

## Шаг 2: Получение данных подключения

После создания вы получите:
- **Host** (например: `db.example.beget.com`)
- **Port** (обычно `5432`)
- **Database** (имя БД)
- **Username** (имя пользователя)
- **Password** (пароль)

## Шаг 3: Выполнение SQL миграции

### Вариант A: Через панель Beget

1. Найдите вашу БД в списке
2. Откройте SQL редактор (если доступен)
3. Скопируйте содержимое файла `database/schema.sql`
4. Выполните SQL

### Вариант B: Через psql (с вашего Mac)

```bash
# Установите psql (если нет)
brew install postgresql

# Подключитесь к БД
psql -h YOUR_HOST -U YOUR_USERNAME -d YOUR_DATABASE

# В psql выполните:
\i database/schema.sql
# или скопируйте содержимое schema.sql и вставьте
```

### Вариант C: Через сервер Beget

```bash
ssh root@155.212.147.140

# Установите psql (если нет)
apt-get update
apt-get install -y postgresql-client

# Подключитесь и выполните миграцию
psql -h YOUR_HOST -U YOUR_USERNAME -d YOUR_DATABASE -f /var/www/arteconew/database/schema.sql
```

## Шаг 4: Настройка переменных окружения

### На локальной машине:

```bash
cp env.template .env.local
nano .env.local
```

Добавьте:
```env
# Формат: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://your_username:your_password@your_host:5432/your_database
DATABASE_SSL=true
```

### На сервере:

```bash
ssh root@155.212.147.140
cd /var/www/arteconew
nano .env
```

Добавьте те же переменные:
```env
DATABASE_URL=postgresql://your_username:your_password@your_host:5432/your_database
DATABASE_SSL=true
```

## Шаг 5: Проверка подключения

### На сервере:

```bash
cd /var/www/arteconew
pm2 restart arteconew
pm2 logs arteconew
```

Проверьте логи - не должно быть ошибок подключения к БД.

### Проверка через админ-панель:

1. Откройте: http://155.212.147.140:3000/admin
2. Попробуйте добавить товар
3. Если всё работает - БД настроена правильно!

## Формат DATABASE_URL:

```
postgresql://username:password@host:port/database
```

Пример:
```
postgresql://myuser:mypassword@db.example.beget.com:5432/mydb
```

## Важно:

- ✅ Используйте **PostgreSQL 16.4** (не MySQL)
- ✅ Минимум **2 ГБ RAM** для БД
- ✅ `DATABASE_SSL=true` обычно нужен для облачных БД
- ✅ Не коммитьте `.env` файл в Git!

## Хранение изображений:

Для изображений используйте локальное хранилище на сервере:

```bash
# На сервере
mkdir -p /var/www/arteconew/public/images
chmod 755 /var/www/arteconew/public/images
```

Или настройте загрузку через админ-панель (можно добавить позже).

## Troubleshooting:

### Ошибка подключения:

```bash
# Проверьте, что БД доступна с сервера
ssh root@155.212.147.140
psql -h YOUR_HOST -U YOUR_USERNAME -d YOUR_DATABASE
```

### Ошибка "relation does not exist":

Выполните миграцию из `database/schema.sql`

### Ошибка SSL:

Попробуйте `DATABASE_SSL=false` или настройте SSL сертификат
