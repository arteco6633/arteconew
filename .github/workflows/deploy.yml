name: Deploy to Beget

on:
  push:
    branches:
      - main
  workflow_dispatch: # Позволяет запускать вручную из GitHub UI

jobs:
  deploy:
    name: Deploy to Beget Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
        env:
          NODE_ENV: production

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.BEGET_SERVER_IP }}
          username: ${{ secrets.BEGET_SERVER_USER }}
          key: ${{ secrets.BEGET_SSH_KEY }}
          port: 22
          script: |
            set -e
            echo "🚀 Начинаем деплой..."
            
            cd /var/www/arteconew || { echo "❌ Ошибка: директория не найдена"; exit 1; }
            echo "✅ Перешли в директорию проекта"
            
            # Обновляем код из репозитория
            echo "📥 Обновляем код из репозитория..."
            git fetch origin || { echo "❌ Ошибка при fetch"; exit 1; }
            git reset --hard origin/main || { echo "❌ Ошибка при reset"; exit 1; }
            git pull origin main || { echo "❌ Ошибка при pull"; exit 1; }
            echo "✅ Код обновлен"
            
            # Устанавливаем зависимости
            echo "📦 Устанавливаем зависимости..."
            npm ci --production=false || { echo "❌ Ошибка при установке зависимостей"; exit 1; }
            echo "✅ Зависимости установлены"
            
            # Собираем проект
            echo "🔨 Собираем проект..."
            npm run build || { echo "❌ Ошибка при сборке"; exit 1; }
            echo "✅ Проект собран"
            
            # Перезапускаем приложение через PM2
            echo "🔄 Перезапускаем приложение..."
            if pm2 list | grep -q "arteconew"; then
              pm2 restart arteconew || { echo "❌ Ошибка при перезапуске"; exit 1; }
              echo "✅ Приложение перезапущено"
            else
              pm2 start ecosystem.config.js || { echo "❌ Ошибка при запуске"; exit 1; }
              echo "✅ Приложение запущено"
            fi
            pm2 save || { echo "⚠️ Предупреждение: не удалось сохранить PM2"; }
            echo "✅ Деплой завершен успешно!"

      - name: Deployment status
        run: |
          echo "✅ Деплой успешно завершен!"
          echo "Приложение доступно на http://${{ secrets.BEGET_SERVER_IP }}:3000"
