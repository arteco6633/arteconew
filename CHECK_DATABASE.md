# ✅ Проверка работы базы данных

## Шаг 1: Проверка подключения к БД

### На сервере:

```bash
ssh root@155.212.147.140
cd /var/www/arteconew

# Установите зависимости (если еще не установлены)
npm install

# Запустите скрипт проверки
node scripts/test-db-connection.js
```

Скрипт проверит:
- ✅ Подключение к базе данных
- ✅ Наличие всех необходимых таблиц
- ✅ Количество записей в таблицах

## Шаг 2: Проверка через psql (альтернатива)

```bash
# На сервере
psql -h 10.19.0.1 -U arteco -d arteco_db
# Введите пароль: 8926416Salavat1996s@

# В psql выполните:
\dt  # Показать все таблицы
SELECT COUNT(*) FROM products;
SELECT COUNT(*) FROM promo_blocks;
\q   # Выход
```

## Шаг 3: Проверка работы приложения

### 1. Перезапустите приложение:

```bash
cd /var/www/arteconew
pm2 restart arteconew
pm2 logs arteconew --lines 30
```

### 2. Проверьте логи на ошибки:

В логах не должно быть ошибок типа:
- "Database connection error"
- "relation does not exist"
- "password authentication failed"

### 3. Проверьте через браузер:

1. **Откройте админ-панель:**
   - http://155.212.147.140:3000/admin

2. **Попробуйте добавить товар:**
   - Перейдите в "Товары"
   - Нажмите "Добавить товар"
   - Заполните форму и сохраните
   - Если товар сохранился - БД работает! ✅

3. **Проверьте главную страницу:**
   - http://155.212.147.140:3000
   - Товары должны отображаться из БД

## Шаг 4: Проверка через API

```bash
# На сервере или локально
curl http://155.212.147.140:3000/api/products

# Должен вернуться JSON с товарами (может быть пустым массивом)
```

## Возможные проблемы:

### Ошибка "relation does not exist"

**Решение:** Выполните миграцию:
```bash
psql -h 10.19.0.1 -U arteco -d arteco_db -f /var/www/arteconew/database/schema.sql
```

### Ошибка подключения

**Проверьте:**
1. Правильность DATABASE_URL в `.env`
2. Экранирование символа `@` в пароле (`%40`)
3. Доступность БД с сервера (firewall)

### Ошибка "password authentication failed"

**Решение:** Проверьте пароль в `.env` файле

## Быстрая проверка всех компонентов:

```bash
# На сервере выполните:
cd /var/www/arteconew

# 1. Проверка БД
node scripts/test-db-connection.js

# 2. Проверка приложения
pm2 status
pm2 logs arteconew --lines 20

# 3. Проверка API
curl http://localhost:3000/api/products | head -20
```

Если все проверки прошли успешно - база данных настроена правильно! 🎉
